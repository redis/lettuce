/*
 * Copyright (c) 2026-Present, Redis Ltd.
 * All rights reserved.
 *
 * SPDX-License-Identifier: MIT
 */
package io.lettuce.core.api.coroutines

import io.lettuce.core.ExperimentalLettuceCoroutinesApi
import io.lettuce.core.probabilistic.CfInfoValue
import io.lettuce.core.probabilistic.ScanDumpValue
import io.lettuce.core.probabilistic.arguments.CfInsertArgs
import io.lettuce.core.probabilistic.arguments.CfReserveArgs

/**
 * Coroutine executed commands for Cuckoo Filter.
 *
 * @author Gyumin Hwang
 * @param <K> Key type.
 * @param <V> Value type.
 * @see <a href="https://redis.io/docs/latest/develop/data-types/probabilistic/cuckoo-filter/">Redis Cuckoo Filter</a>
 * @since 7.7
 * @generated by io.lettuce.apigenerator.CreateKotlinCoroutinesApi
 */
@ExperimentalLettuceCoroutinesApi
interface RedisCuckooFilterCoroutinesCommands<K : Any, V : Any> {

    /**
     * Creates an empty Cuckoo filter with a single sub-filter for the initial specified capacity.
     *
     * @param key the key.
     * @param capacity the capacity.
     * @return String simple-string-reply `OK` if `CF.RESERVE` was executed correctly.
     */
    suspend fun cfReserve(key: K, capacity: Long): String?

    /**
     * Creates an empty Cuckoo filter with a single sub-filter for the initial specified capacity.
     *
     * @param key the key.
     * @param capacity the capacity.
     * @param args the reserve arguments.
     * @return String simple-string-reply `OK` if `CF.RESERVE` was executed correctly.
     */
    suspend fun cfReserve(key: K, capacity: Long, args: CfReserveArgs): String?

    /**
     * Add an item to the Cuckoo Filter. A filter will be created if one does not exist.
     *
     * @param key the key.
     * @param value the value.
     * @return Boolean integer-reply `true` if the item was added, `false` if it was already in the filter.
     * @throws io.lettuce.core.RedisCommandExecutionException if the filter is full and cannot expand.
     */
    suspend fun cfAdd(key: K, value: V): Boolean?

    /**
     * Add an item to the Cuckoo Filter only if the item does not already exist. A filter will be created if one does not exist.
     *
     * @param key the key.
     * @param value the value.
     * @return Boolean integer-reply `true` if the item was added, `false` if it was already in the filter.
     */
    suspend fun cfAddNx(key: K, value: V): Boolean?

    /**
     * Add one or more items to a Cuckoo Filter. A filter will be created if one does not exist.
     *
     * @param key the key.
     * @param value the value.
     * @return List<Boolean> one entry per item: `true` if the item was added, `false` if the filter is full.
     *         CF.INSERT does not report already-existing items.
     */
    suspend fun cfInsert(key: K, value: V): List<Boolean>

    /**
     * Add one or more items to a Cuckoo Filter. A filter will be created if one does not exist.
     *
     * @param key the key.
     * @param args the insert arguments.
     * @param value the value.
     * @return List<Boolean> one entry per item: `true` if the item was added, `false` if the filter is full.
     *         CF.INSERT does not report already-existing items.
     */
    suspend fun cfInsert(key: K, args: CfInsertArgs, value: V): List<Boolean>

    /**
     * Add one or more items to a Cuckoo Filter. A filter will be created if one does not exist.
     *
     * @param key the key.
     * @param values the values.
     * @return List<Boolean> one entry per item: `true` if the item was added, `false` if the filter is full.
     *         CF.INSERT does not report already-existing items.
     */
    suspend fun cfInsert(key: K, vararg values: V): List<Boolean>

    /**
     * Add one or more items to a Cuckoo Filter. A filter will be created if one does not exist.
     *
     * @param key the key.
     * @param args the insert arguments.
     * @param values the values.
     * @return List<Boolean> one entry per item: `true` if the item was added, `false` if the filter is full.
     *         CF.INSERT does not report already-existing items.
     */
    suspend fun cfInsert(key: K, args: CfInsertArgs, vararg values: V): List<Boolean>

    /**
     * Add one or more items to a Cuckoo Filter only if the items do not already exist. A filter will be created if one does not
     * exist.
     *
     * @param key the key.
     * @param value the value.
     * @return List<Long> one entry per item: `1` if the item was added, `0` if it already exists, `-1` if the filter is full.
     */
    suspend fun cfInsertNx(key: K, value: V): List<Long>

    /**
     * Add one or more items to a Cuckoo Filter only if the items do not already exist. A filter will be created if one does not
     * exist.
     *
     * @param key the key.
     * @param args the insert arguments.
     * @param value the value.
     * @return List<Long> one entry per item: `1` if the item was added, `0` if it already exists, `-1` if the filter is full.
     */
    suspend fun cfInsertNx(key: K, args: CfInsertArgs, value: V): List<Long>

    /**
     * Add one or more items to a Cuckoo Filter only if the items do not already exist. A filter will be created if one does not
     * exist.
     *
     * @param key the key.
     * @param values the values.
     * @return List<Long> one entry per item: `1` if the item was added, `0` if it already exists, `-1` if the filter is full.
     */
    suspend fun cfInsertNx(key: K, vararg values: V): List<Long>

    /**
     * Add one or more items to a Cuckoo Filter only if the items do not already exist. A filter will be created if one does not
     * exist.
     *
     * @param key the key.
     * @param args the insert arguments.
     * @param values the values.
     * @return List<Long> one entry per item: `1` if the item was added, `0` if it already exists, `-1` if the filter is full.
     */
    suspend fun cfInsertNx(key: K, args: CfInsertArgs, vararg values: V): List<Long>

    /**
     * Check if an item exists in the Cuckoo Filter.
     *
     * @param key the key.
     * @param value the value.
     * @return Boolean integer-reply `true` if the item may exist in the filter, `false` if it definitely does not.
     */
    suspend fun cfExists(key: K, value: V): Boolean?

    /**
     * Check if one or more items exist in the Cuckoo Filter.
     *
     * @param key the key.
     * @param values the values.
     * @return List<Boolean> of `true` or `false` where `true` means that, with high probability, the item was already added to
     *         the filter, and `false` means that the item was definitely not added to the filter.
     */
    suspend fun cfMExists(key: K, vararg values: V): List<Boolean>

    /**
     * Delete an item from the Cuckoo Filter.
     *
     * **Warning:** Deleting an item that was not previously added to the filter may cause false negatives for items that are
     * actually present. Only delete items that have been previously added.
     *
     * @param key the key.
     * @param value the value.
     * @return Boolean integer-reply `true` if the item was deleted, `false` if the item was not found.
     */
    suspend fun cfDel(key: K, value: V): Boolean?

    /**
     * Return the number of times an item may be in the Cuckoo Filter.
     *
     * @param key the key.
     * @param value the value.
     * @return Long integer-reply the number of times the item may be in the filter.
     */
    suspend fun cfCount(key: K, value: V): Long?

    /**
     * Begins an incremental save of the Cuckoo filter.
     *
     * @param key the key.
     * @param cursor the cursor.
     * @return ScanDumpValue the scan dump value.
     */
    suspend fun cfScanDump(key: K, cursor: Long): ScanDumpValue?

    /**
     * Restores a Cuckoo filter previously saved using CF.SCANDUMP.
     *
     * @param key the key.
     * @param cursor the cursor.
     * @param data the data.
     * @return String simple-string-reply `OK` if `CF.LOADCHUNK` was executed correctly.
     */
    suspend fun cfLoadChunk(key: K, cursor: Long, data: ByteArray): String?

    /**
     * Get information about the Cuckoo Filter.
     *
     * @param key the key.
     * @return CfInfoValue the information about the filter.
     */
    suspend fun cfInfo(key: K): CfInfoValue?

}
